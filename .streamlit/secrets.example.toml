# Copy these values into Streamlit Cloud > App settings > Secrets.
# Do not rename this file to secrets.toml in Git.

GEMINI_API_KEY = "replace-with-your-gemini-api-key"

DB_HOST = "replace-with-external-mysql-host"
DB_PORT = "3306"
DB_USER = "replace-with-db-user"
DB_PASSWORD = "replace-with-db-password"
DB_NAME = "scc_osp2026"

ADMIN_USERNAME = "admin"
ADMIN_PASSWORD = "replace-with-admin-password"

# Upload kobert_v1_1800.zip to GitHub Releases, Hugging Face, or another direct download host.
KOBERT_MODEL_ZIP_URL = "https://example.com/kobert_v1_1800.zip"

# Optional but recommended. Generate locally with:
# PowerShell: Get-FileHash .\kobert_v1_1800.zip -Algorithm SHA256
KOBERT_MODEL_ZIP_SHA256 = ""
